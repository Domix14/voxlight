#version 330 core
layout(location = 0) in vec3 vertexPosition_modelspace;

void main() {
    vec3 lookAt = vec3(0,0,0);
    camPos2 = vec3(30, 0, 0);
    camDir = normalize(lookAt - camPos);

    gl_Position.xyz = vertexPosition_modelspace;
    gl_Position.w = 1.0;
}